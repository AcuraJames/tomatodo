# FIX.md — История правок

## Версия 0.1.7
- **Select/input в Zen-режиме**: исправлены цвета — на светлой теме тёмный текст, на тёмной тёмный фон (inline-стили через `getSelectStyle()`)
- **Toggle Zen mode**: сделан видимым на светлом фоне (`bg-white/80` + `border-gray-400`)
- **KoiFishCanvas** (первая версия):
  - ResizeObserver для адаптивного размера Canvas
  - Рыбки на относительной орбите (`0.26–0.36` от `min(w,h)`)
  - Размер рыбок уменьшен (`9 + rand(3)`)
  - Круги-волны (ripple) с theme-aware цветом
  - Убрано `w-full h-full` (только `inset-0`)
- **ZenBackground**: добавлен ResizeObserver
- **Рыбки в пруду Zen**: 3 маленькие рыбки (простая анимация)
- **Версия**: 0.1.7

## Версия 0.1.8 — Break + правки рыб
- **Break-логика**:
  - `timerStore.mode`: 'focus' / 'break'
  - `startBreak()`, `skipBreak()`, `breakDuration` (5 мин)
  - `tick()` только декрементирует (без авто-перехода)
  - Watcher в TimerView: focus-end → complete session → startBreak(); break-end → reset()
  - Зелёный круг прогресса (`text-green-500`) во время break
  - Текст «Отдыхаю» / «Пауза отдыха»
  - Кнопка «Прервать отдых» (с очисткой interval)
- **Рыбки кои**: орбита расширена до `0.38–0.48`, размер восстановлен (`13 + rand(5)`), возвращены `w-full h-full`
- **Рыбки в пруду Zen**: исправлена анимация — круговой путь, `atan2` для направления, body-wave, tail-wag
- **Toggle Zen**: `border` → `border-2` для видимости на светлой теме
- **Версия**: 0.1.8
- **Git**: инициализирован репозиторий, первый push в `github.com:AcuraJames/tomatodo.git`

## Версия 0.1.10 — Задачи, dueDate, настройки
- **Создание задачи в таймере**:
  - Кнопка «+ Новая задача» в блоке выбора задачи
  - Форма: input названия, select проекта (по умолч. «Входящие»), Сохранить/Отмена
  - Проверка на пустую строку
  - После сохранения задача подставляется в селект
- **dueDate в Task**:
  - Поле `dueDate?: string` в типе Task
  - `determineListType()`: today → 'today', 1–6d → 'week', else → 'inbox'
  - Дата автоматически определяет список
- **Календарь**: `input type="date"` при создании задачи (TimerView и TasksView)
- **Отображение даты**: карточки задач показывают ДД.ММ.ГГ
- **Просроченные задачи**: цвет даты — красный (`text-red-500`)
- **Название проекта на карточке**: отображается рядом с задачей в списках
- **Автозапуск следующей сессии**:
  - `autoStartNextSession` (default: true) в settingsStore
  - После break → если включено → автозапуск нового фокуса
  - Настройка в блоке «Таймер» в SettingsView
- **Версия**: 0.1.10

## Версия 0.1.11 — UI правки, Tag, календарь
- **Блок задачи на таймере**:
  - Label убран, «Задача» — дефолтный пункт селекта
  - Кнопка «+» переделана в инлайн (рядом с селектом)
  - Попап: GlassCard-оверлей (input + select проекта + сохранить/отмена) по клику на «+»
  - Теги проекта и даты внутри селекта (кастомный select: native select с opacity-0 поверх кастомного div)
  - Названия проектов из option вычеркнуты (заменены тегами)
  - Кнопка «+» выровнена по высоте (`px-3 py-2.5`)
- **Tag.vue** — новый компонент:
  - `text` + `color`, фон `color+1A`
  - Цвета сроков: сегодня `#ef4444`, 1–6д `#f59e0b`, 7+д `#22c55e`
  - Цвет проекта — заданный при создании проекта
- **Календарь**: удалён из TasksView (полностью)
- **Списки/Проекты**:
  - Теги проекта только в списках (inbox/today/week), не в проекте
  - `determineListType` исправлен: overdue → today, today → today, 1–6d → week, else → inbox
- **Версия**: 0.1.11

## Версия 0.1.12 — Баг-фиксы, shared utils, тесты
- **Просроченные задачи**: исправлен цвет тега — `#ef4444` (красный), ранее ошибочно был зелёный
- **`visibilitychange`**: break-сессии больше не убиваются при сворачивании вкладки
- **`createTask` в таймере**: projectId для «Входящих» — пустая строка, не строка `'inbox'`
- **`determineListType`**: просроченные задачи (`diffDays < 0`) попадают в `today`, ранее — в `inbox`
- **Shared-утилиты**: `getDateTagColor`, `formatDate`, `getProjectName`, `getProjectColor` вынесены в `src/utils/helpers.ts`
- **Уведомления**: `useNotification` — браузерные уведомления при завершении фокуса/перерыва
- **Восстановление таймера**: `lastTickAt` + `afterHydrate` — таймер корректно показывает оставшееся время после перезагрузки
- **404 маршрут**: catch-all редиректит на `/`
- **Тесты**: Vitest + 52 unit-теста (helpers, tasksStore, timerStore)
- **Версия**: 0.1.12

## Версия 0.1.14 — Темы, календарь, попап задач
- **Темы в селекте таймера**: исправлены цвета — в тёмной теме корректный фон (`#1a1a2e`) и текст (`#e2e8f0`)
- **Попап создания задачи в таймере**: переделан под дизайн ProjectModal (центрированный модал, backdrop-blur, крестик)
- **Кастомный календарь** (`CalendarPicker`): минималистичный компонент с навигацией по месяцам, выделением today и выбранной даты
- **Автоматическое определение списка**: задача с датой автоматически попадает в нужный список (today/week/inbox)
- **Календарь в ListsView**: отображается во всех списках кроме «Сегодня»
- **Тесты**: расширены до 75 unit-тестов (calendarPicker + addTask edge cases)
- **Версия**: 0.1.14

## Версия 0.1.15 — Темы, календарь, попап задач, логика списков
- **Темы в селекте таймера**: исправлены — `bg-white dark:bg-[#1a1a2e]` + `text-text`/`text-text-secondary`
- **Попап создания задачи в таймере**: новый дизайн — ProjectModal стиль (центрированный модал, backdrop-blur, крестик)
- **Кастомный календарь** (`CalendarPicker`): блокировка прошлых дат, кнопка «Назад» на текущем месяце
- **Список «На этой неделе» → «Планы»**: все датированные задачи (кроме сегодня/входящих)
- **`determineListType(dueDate?, context?)`**: context явно определяет список, без fallback на listType
- **`updateTask`**: пересчёт listType при изменении dueDate (кроме проектов)
- **`addTask`**: context-only логика, listType параметр только для совместимости
- **Восстановление таймера**: `recoverFromReload` — пауза вместо сброса при перезагрузке
- **404 страница**: стилизованная страница «Страница не найдена» с кнопками навигации
- **AppLayout**: сайдбар — «Планы» вместо «На этой неделе», путь `/tasks/plans`
- **Router**: 404 → NotFoundView (не редирект), маршрут `/:pathMatch(.*)*`
- **Тесты**: 78 unit-тестов, localStorage.clear() в beforeEach
- **Правила назначения списков**: `RULES.md` документирует все business rules
- **Roadmap**: `MAP.md` → `ROADMAP.md`, разделение на Done/To Do
- **Версия**: 0.1.15
